{% extends "AccueilBundle::layout.html.twig" %}

{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block title %}
    Annonces - {{ parent() }}
{% endblock %}

{% block body %}
    <div class="menu">
        <div class="row">
            <div class="jumbotron">
                <h2>Choississez votre type de billets</h2>
                <h3>{{ date }}</h3>
                <h3>Demi journée : {{ dj }}</h3>
                {{ form_start(form) }}
                {{ form(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}